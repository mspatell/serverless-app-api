{
  "name": "notes-crud-api",
  "version": "1.0.0",
  "description": "Notes CRUD API using API Gateway and AWS Lambda",
  "main": "api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Meetkumar Patel",
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.363.0",
    "@aws-sdk/util-dynamodb": "^3.363.0",
    "jest": "^29.5.0",
    "serverless-iam-roles-per-function": "^3.2.0"
  }
}
